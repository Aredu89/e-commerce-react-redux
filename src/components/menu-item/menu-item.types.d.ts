export type MenuItemProps = {
  title: string;
  imageUrl: string;
  linkUrl: string;
  size?: string;
};

export type BackgroundImageProps = {
  imageUrl: string;
};

export type MenuItemContainer = {
  size?: string;
};
